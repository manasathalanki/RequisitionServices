server:
  port: 8081
  servlet:
    context-path: /dashboard
eureka:
  instance:
    hostname:  ${EUREKA_HOST_NAME:localhost}
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8090}/eureka/ 

spring:
  application:
    name: recruit-services-dashboard